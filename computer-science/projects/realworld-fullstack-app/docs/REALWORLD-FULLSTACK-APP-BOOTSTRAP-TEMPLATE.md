# Realworld Fullstack App's Bootstrap Template [see it live on gh-pages](http://TexasBullet26.github.io/#/)

---

1. Pull repo
2. Update bootstrap submodule
3. Run `npm install` within the pulled repo's root directory
4. Run `gulp`
